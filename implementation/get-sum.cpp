#include <bits/stdc++.h>
using namespace std;
int get_sum( int n, int m)
{
    pair <int, int> ans;
    int a,b;
   // tie(a,b) = pair1 <int,int> pr;//
    pair<set<int> >
    if(n > m)
     ans;
    else
    {
        
        for(int i=0; i<n; i++)
        {
            if( m==0)
            return 0;
        }
    }
}
int get_sum(int n, int m)
{
    int sum=0;
    if( n% 2==0)
    {
        sum+= n/2;
         while( n!=0)
         {
            n--;

         }
    }
}